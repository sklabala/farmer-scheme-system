{
  "name": "farmer-scheme-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start",
    "start": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "puppeteer": "^24.40.0"
  }
}
